============
Installation
============

At the command line::

    $ easy_install django-dateadd

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-dateadd
    $ pip install django-dateadd
